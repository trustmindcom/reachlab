# Spike Examples

Anonymized examples of LinkedIn Voyager API responses used during the discovery spike.

These show the response shapes — actual personal data should never be committed.

## Files

- `profile-response.json` — Profile lookup response shape (Voyager recipe 766)

## Where real spike data lives

Real spike data with personal info is gitignored in the parent `spike/` directory.
Keep personal API response captures there for local reference.
